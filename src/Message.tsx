/**
 * Pascal Case
 * @returns
 */
function Message() {
  const name = "shareef";
  //JSX javascript xml
  if (name) return <h1>Hey {name} Hello World!</h1>;

  return <h1>Hello World!</h1>;
}

export default Message;
